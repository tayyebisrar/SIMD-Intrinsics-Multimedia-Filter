#include <cmath>
#include "bmp.h"

void grayscale(IMAGE_DATA &image);
void sepia(IMAGE_DATA &image);
void horizontal_reflect(IMAGE_DATA &image);
void blur(IMAGE_DATA &image);
void edges(IMAGE_DATA &image);

void grayscale(IMAGE_DATA &image){
    // using Compiler Intrinsics
}